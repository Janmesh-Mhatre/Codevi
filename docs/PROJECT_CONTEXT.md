# Project Context

Always assume:

-   Project name: Codevi
-   Continue from the existing repository.
-   Never recreate the project.
-   Preserve previous functionality.
-   Implement only the requested phase.
-   Browser-based development until all phases are complete.
-   Convert to native desktop (.exe) only after the final phase.
-   Fix regressions before implementing the next phase.
